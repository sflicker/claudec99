# ClaudeC99 self compile validation

## Task
self compile and validate

#Note this stage was previous attempted but was stopped due to a blocker.#

Using the existing version (C0) of the compiler use it to create a new version of itself (C1).
Then run all of the tests using C1 and verify all pass.

If this passes then update the MINOR version number in version.c and use C1 to create
a new version C2 and repeat running of the tests.

If these all pass update the README.md to include that this project has achieved self-compilation.

