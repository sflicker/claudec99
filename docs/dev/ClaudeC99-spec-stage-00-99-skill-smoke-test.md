# ClaudeC99 Stage 00-99

## Task

This is a smoke test for the `implement-stage` skill only.

Do not modify compiler source files.

Do not update `docs/grammar.md`.

Do not update `README.md`.

Do not run the full compiler implementation workflow.

The expected behavior is:

1. Read this spec.
2. Derive `STAGE_LABEL`.
3. Read the generated artifact rules.
4. Produce a Kickoff Artifact preview.
5. Produce a Planned Changes section saying no implementation changes are required.
6. Stop before implementation.

## Expected Result

The skill should demonstrate that templates, references, and stage-label processing work.