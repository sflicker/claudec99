# stage 86 usage  (i experimented with the /tui fullscreen command which seems to have reset the stats)

  Session

  Total cost:            $0.0000
  Total duration (API):  0s
  Total duration (wall): 3s
  Total code changes:    0 lines added, 0 lines removed
  Usage:                 0 input, 0 output, 0 cache read, 0 cache write

  Current session             
  ██████████████                                     28% used
  Resets 12:50am (America/New_York)

  Current week (all models)   
  █▌                                                 3% used
  Resets Jun 5, 10am (America/New_York)

  What's contributing to your limits usage?
  Approximate, based on local sessions on this machine — does not include other devices or claude.ai
                              
  Last 24h · these are independent characteristics of your usage, not a breakdown

  100% of your usage came from subagent-heavy sessions
   Each subagent runs its own requests. Be deliberate about spawning them — and
   consider configuring a cheaper model for simpler subagents.

  33% of your usage was at >150k context
   Longer sessions are more expensive even when cached. /compact mid-task, /clear
   when switching to new tasks.

  97% of your usage came from /implement-stage
   Heavy skills can be scoped down or run with a cheaper model via skill
   frontmatter.

  Skills                  % of usage
  /implement-stage               97%

  Subagents               % of usage
  implement-stage                 2%

  d to day · w to week


