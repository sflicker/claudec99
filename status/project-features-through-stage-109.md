What works today: a full preprocessing pipeline (line splicing, comment removal, `#include "file.h"` and `#include <file.h>`, object-like and function-like `#define` with parameter substitution and recursive expansion, variadic macros with `__VA_ARGS__`, macro stringification `#param`, token pasting `##`, `#undef`, conditional compilation `#ifdef`/`#ifndef`/`#if`/`#elif`/`#else`/`#endif`/`#elifdef`/`#elifndef` (stage 108) with full recursive-descent expression evaluation including `defined()`, macro expansion, arithmetic, bitwise, shift, logical, comparison operators, and ternary, `#error`, predefined macros `__FILE__`/`__LINE__`/`__DATE__`/`__TIME__`/`__STDC__`/`__STDC_VERSION__`/`__STDC_HOSTED__`/`__CLAUDEC99__`, ABI-reflecting macros `__x86_64__`/`__linux__`/`__unix__`/`__LP64__`/`_LP64`/`__CHAR_BIT__`/the full `__SIZEOF_*__` family, command-line `-D`/`-I`/`--sysroot` flags, unknown `#pragma` silently ignored); a comprehensive set of stub system headers in `test/include/` covering `stdio.h` (opaque `FILE`, `EOF`, `fopen`, `fclose`, `fgetc`, `fgets`, `fprintf`, `snprintf`, `puts`, `printf`, `putchar`, the `vfprintf`/`vprintf`/`vsnprintf` variadic-forwarding family, standard streams `stdin`/`stdout`/`stderr`, file-position/read stubs `fseek`/`ftell`/`fread`/`fwrite` with `SEEK_*`, and 31 additional I/O functions including `feof`, `ferror`, `clearerr`, `remove`, `rename`, `tmpfile`), `stddef.h`, `stdlib.h` (`malloc`, `calloc`, `free`, `realloc`, `exit`, `abort`, `_Exit`, `strtol`, `strtoul`, `strtoll`, `strtoull`, `atoi`, `atol`, `atoll`, `abs`, `labs`, `llabs`, `div`/`ldiv`/`lldiv` with their `_t` types, `qsort`, `bsearch`, `rand`, `srand`, `atexit`, `system`, `getenv`, `EXIT_SUCCESS`, `EXIT_FAILURE`, `RAND_MAX`), `string.h` (`strlen`, `strcmp`, `strcpy`, `memcpy`, `memset`, `memcmp`, `strchr`, `strncat`, `strncmp`, `strncpy`, `strrchr`, `memmove`, `memchr`, `strcat`, `strstr`, `strtok`, `strerror`, and more), `limits.h` (full set with `LLONG_MIN`/`LLONG_MAX`/`ULLONG_MAX`/`CHAR_MIN`/`CHAR_MAX`/`MB_LEN_MAX`), `stdint.h` (exact-width/pointer-size/fast/least integer typedefs), `stdbool.h` (`bool`, `true`, `false`), `ctype.h` (full character classification: `isalpha`, `isdigit`, `isspace`, `iscntrl`, `isgraph`, `isprint`, `ispunct`, and more), `errno.h` (`errno`, `ERANGE`, `EINVAL`, …), `time.h` (`time_t`, time functions), `setjmp.h` (non-local jumps), `stdarg.h` (`va_list` plus the `va_start`/`va_end`/`va_copy`/`va_arg` macros), and `assert.h` (NDEBUG-aware `assert` macro — stage 107); statements (if/else, while/do-while/for **including C99 `for`-init declarations** `for (int i = 0; …)`, switch/case/default with **full constant-expression case labels** — integer/character literals, enum constants, and all of `* / % << >> + - & ^ |` with unary `~ !`, relational `< <= > >=`, equality `== !=`, logical `&& ||`, ternary `?:`, and parentheses (stage 77; completed stage 104), break/continue/goto, blocks with shadowing, expression statements); the full signed integer hierarchy (`char`/`short`/`int`/`long`) plus all four unsigned variants plus `long long` and `unsigned long long`, the `signed` keyword forms and trailing-`int` forms; `_Bool` with value-normalization and integer promotion; **`float` (size=4, align=4) and `double` (size=8, align=8) scalar types** (stage 109) — local declarations with FP literal initializers, global declarations (`.data` `dd`/`dq`, `.bss` `resd`/`resq`), struct/union float/double member declaration and assignment, implicit float→double widening via `cvtss2sd`, per-TU `.rodata` FP literal pool (`Lfc<N>: dd/dq` labels); floating-point literal forms (stage 109): decimal digits with optional fraction, optional `e`/`E` exponent, optional `f`/`F` suffix; leading-dot form; `f`/`F` suffix yields float, no suffix yields double; unsigned-aware arithmetic, division, shifts, and comparisons; integer promotions and Usual Arithmetic Conversions for mixed signed/unsigned operands including LP64-specific rules; decimal and **`0x`/`0X` hexadecimal** integer literals with `U`/`L`/`UL`/`LL`/`ULL` suffixes and overflow-aware typing; void type and `void*` generic pointer; `const` qualifier on base-type scalars, **pointer-level `const` enforcement**, and **`const` in struct/union members and type-name contexts**; **minimal `volatile` handling** (tokenized, parsed everywhere `const` is, tracked on types, no codegen effect yet); `restrict` qualifier (stage 106, parse-and-ignore); `inline` keyword (stage 107, parse-and-ignore); casts; multi-function programs with forward declarations; **variadic function definitions** with `<stdarg.h>` support — `va_list`, `va_start`, `va_end`, `va_arg` for GP-class types, and **functional `va_copy`** (24-byte SysV AMD64 `va_list` struct copy — stage 107); **struct and union parameters and return values passed by value** per the System V AMD64 ABI (register-class aggregates ≤16 bytes in registers, memory-class aggregates >16 bytes through a hidden pointer; `rep movsb` block copies; whole-struct assignment and decl-init from struct rvalues); **function calls with an arbitrary number of arguments** (full System V AMD64 stack-passing); pointers (declarations, `&`/`*` as rvalue and lvalue, assign-through-pointer, pointer parameters and return types, full pointer arithmetic including difference, `NULL`, null-constant `0`, logical `!` on pointer operands); arrays (declarations, **multidimensional arrays** up to 8 dimensions, indexing, decay-to-pointer including struct/union member-array decay, file-scope brace/string initializers with size inference); **designated initializers** — `.member = value` and `[index] = value` at both local and global scope, mixing designated and non-designated elements; **compound literals** — `(struct T){ … }`, `(int[]){ … }`, `(int[N]){ … }`, `(int){ expr }` stack temporaries; string and character literals with simple/hex `\xNN`/octal `\NNN` escapes and **adjacent string-literal concatenation**; all bitwise operators, remainder, and all eleven compound assignment operators on any modifiable lvalue; prefix/postfix `++`/`--` on any modifiable lvalue; unary `&` on any addressable lvalue; `sizeof`; conditional (ternary) and comma operators; comma-separated init-declarator lists; file-scope global variable declarations; `extern` and `static` storage-class specifiers at file scope and **block-scope `static` local variables** including scalars, arrays, structs, and unions with brace initializers, designated initializers, and full **constant-expression initializers** (stage 103); function pointer declarations/assignment/indirect calls; `typedef` aliases; **`enum` declarations** with full integer constant expression values (`FLAG = 1 << 0`, arithmetic, bitwise, shift, relational, logical, ternary operators) and **forward-declared enum tags** (stage 99); structs and unions (definition, access, designated initializers, whole-aggregate assignment/copy, nested types, arrays of structs, typedef aliases, incomplete forward declarations, **anonymous struct/union type declarations**); **integer constant expression evaluator** spanning both token-stream and AST contexts: the full C99 operator set including `| ^ & + - << >> * / %`, unary `+ - ~ !`, relational `< <= > >=`, equality `== !=`, logical `&& ||`, ternary `?:`, `sizeof(type-name)`, enum constants, and integer/character literals (stages 77–104, completing in stage 104); **file-scope integer constant-expression initializers** (`int x = 1 + 2;`, `sizeof`-based — stage 100); **multi-file compilation** (stage 96); **designated initializers** (stage 97); **compound literals** (stage 98). Diagnostics: `<file>:<line>:<col>:` position prefixes, `--max-errors=N` multi-error collection with setjmp/longjmp recovery, `--version` build identifier, `-Werror`; `--print-ast`/`--print-tokens`/`--print-asm` developer modes. The compiler's own source is strict ISO C99, the compiler is **fully self-hosting** (C0→C1→C2, all 1627 tests passing at each step — stage 92; updated through stage 109). Stage 95-01 through 95-12 replaced all fixed-capacity tables with `Vec`/`StrBuf` dynamic containers and moved all name text into a lexer-owned string pool.

What's still out of scope: **float/double arithmetic, comparisons, and function parameters/return values** (Stages 110–112); `va_arg` for floating-point and struct-by-value types; multi-character and wide-character literals; compound literals at file scope; bit-fields, flexible array members; `volatile` code-generation semantics (currently parsed and tracked only); chained designators (`.a.b`, `.arr[0]`) and designated union init for non-first members; functions returning function pointers; pointer-to-array declarators (`(*p)[10]`); old-style (K&R) function definitions; `__attribute__` specifiers; GNU variadic macro extensions (`__VA_OPT__`, named variadic args, comma deletion); object-file (`.o`) emission and separate linking; header-only precompilation.
