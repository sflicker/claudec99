What works today: a full preprocessing pipeline (line splicing, comment removal, `#include "file.h"` and `#include <file.h>`, object-like and function-like `#define` with parameter substitution and recursive expansion, variadic macros with `__VA_ARGS__`, macro stringification `#param`, token pasting `##`, `#undef`, conditional compilation `#ifdef`/`#ifndef`/`#if`/`#elif`/`#else`/`#endif` with full recursive-descent expression evaluation including `defined()`, macro expansion, arithmetic, bitwise, shift, logical, and comparison operators, `#error`, `#elifdef`/`#elifndef` (stage 108), predefined macros `__FILE__`/`__LINE__`/`__DATE__`/`__TIME__`/`__STDC__`/`__STDC_VERSION__`/`__STDC_HOSTED__`/`__CLAUDEC99__` (with `__FILE__`/`__LINE__` fixed inside nested `#include` chains — stage 107), ABI-reflecting macros `__x86_64__`/`__linux__`/`__unix__`/`__LP64__`/`_LP64`/`__CHAR_BIT__`/the full `__SIZEOF_*__` family, command-line `-D`/`-I`/`--sysroot` flags); an expanded set of stub system headers in `test/include/` covering `stdio.h` (opaque `FILE`, `EOF`, `fopen`, `fclose`, `fgetc`, `fgets`, `fprintf`, `snprintf`, `puts`, `printf`, `putchar`, the `vfprintf`/`vprintf`/`vsnprintf` variadic-forwarding family, the standard streams `stdin`/`stdout`/`stderr`, and the file-position/read stubs `fseek`/`ftell`/`fread` with `SEEK_*`), `stddef.h`, `stdlib.h` (`malloc`, `calloc`, `free`, `realloc`, `exit`, `strtol`, `strtoul`, `strtod`, `atoi`, `qsort`, `getenv`, `abs`), `string.h` (`strlen`, `strcmp`, `strcpy`, `memcpy`, `memset`, `memcmp`, `strchr`, `strncat`, `strncmp`, `strncpy`, `strrchr`), `limits.h` (with `LLONG_MIN`/`LLONG_MAX`/`ULLONG_MAX`), `stdint.h` (exact-width/pointer-size/fast/least integer typedefs), `stdbool.h` (`bool`, `true`, `false`), `ctype.h` (character classification), `errno.h` (`errno`, `ERANGE`, `EINVAL`, …), `time.h` (`time_t`, time functions), `setjmp.h` (non-local jumps), `stdarg.h` (`va_list` plus the `va_start`/`va_end`/`va_copy`/`va_arg` macros), and `assert.h` (`assert()` macro — stage 107); statements (if/else, while/do-while/for **including C99 `for`-init declarations** `for (int i = 0; …)`, switch/case/default with **full constant-expression case labels** — integer/character literals, enum constants, and all of `?: || && == != < <= > >= << >> + - * / % & ^ |` with unary `~ !` and parentheses (stage 77; extended stages 99, 104), break/continue/goto, blocks with shadowing, expression statements); the full signed integer hierarchy (`char`/`short`/`int`/`long`) plus all four unsigned variants plus `long long` and `unsigned long long`, the `signed` keyword forms and trailing-`int` forms; `_Bool` with value-normalization and integer promotion; unsigned-aware arithmetic, division, shifts, and comparisons; integer promotions and Usual Arithmetic Conversions for mixed signed/unsigned operands including LP64-specific rules; decimal and **`0x`/`0X` hexadecimal** integer literals with `U`/`L`/`UL`/`LL`/`ULL` suffixes and overflow-aware typing; **`float` and `double` scalar types** (stage 109) — 4-byte/8-byte SSE2 values in `xmm0`; float/double literals in decimal notation with `f`/`F` suffix (float) or no suffix (double), deduplicated `.rodata` pool with `Lfc<N>` labels; `sizeof(float)` = 4, `sizeof(double)` = 8; `movss`/`movsd` load/store; **FP binary arithmetic** `addss`/`addsd`, `subss`/`subsd`, `mulss`/`mulsd`, `divss`/`divsd` with save/restore convention using the stack (stage 110); **FP unary negation** via 16-byte-aligned sign-mask XOR (`Lfp_smask_f32`/`Lfp_smask_f64`); **Usual Arithmetic Conversions for FP** via `fp_common_arith_kind` (double wins over float wins over int); **implicit FP conversions** `cvtsi2ss`/`cvtsi2sd` (int→FP), `cvtss2sd`/`cvtsd2ss` (between FP widths); **explicit FP casts** `(float)`, `(double)`, `(int)` via truncating `cvttss2si`/`cvttsd2si`; **FP comparison operators** `<`, `<=`, `>`, `>=`, `==`, `!=` via `ucomiss`/`ucomisd` with NaN-correct sequences (`sete+setnp+and` for `==`, `setne+setp+or` for `!=`, ordered set* codes for relational) (stage 111); **FP boolean context** in `if`/`while`/`for`/ternary/`!` via `emit_fp_bool_to_rax` (compare to zero, NaN treated as truthy, `!NaN == 0`); **mixed FP/int comparisons** promoting the integer side; void type and `void*` generic pointer; `const` qualifier on base-type scalars, **pointer-level `const` enforcement**, and **`const` in struct/union members and type-name contexts** (writes through `const*`, const-pointer reassignment, assignment to a const member directly/via subscript/through a pointer to a const object, and const-discard conversions all diagnosed); **minimal `volatile` handling** (tokenized, parsed everywhere `const` is, tracked on types, no codegen effect yet); **`restrict` qualifier** (tokenized, parsed at all pointer-qualifier positions, silently discarded — stage 106); **`inline` function specifier** (tokenized, parsed in `parse_declaration_specifiers`, silently discarded — stage 107); casts; multi-function programs with forward declarations; **variadic function definitions** with `<stdarg.h>` support — `va_list`, `va_start`, `va_end`, and `va_arg` for GP-class types (`int`/`unsigned`/`long`/`unsigned long`/`long long`/`unsigned long long`/pointer) via a SysV AMD64 register save area with both register and overflow-stack paths; **`va_copy` functional codegen** (three 8-byte copy instructions for `__builtin_va_copy` — stage 107); **struct and union parameters and return values passed by value** per the System V AMD64 ABI (register-class aggregates ≤16 bytes in registers, memory-class aggregates >16 bytes through a hidden pointer; `rep movsb` block copies; whole-struct assignment and decl-init from struct rvalues, including subscript/dot/arrow/deref targets); **function calls with an arbitrary number of arguments** (full System V AMD64 stack-passing on both caller and callee sides, with 16-byte alignment); pointers (declarations, `&`/`*` as rvalue and lvalue, assign-through-pointer, pointer parameters and return types, full pointer arithmetic including difference yielding `long`, `NULL`, null-constant `0` accepted as a pointer argument, and logical `!` on pointer operands); arrays (declarations, **multidimensional arrays** up to 8 dimensions in declarations/indexing/member access/`sizeof`, indexing, multi-level subscript, decay-to-pointer including **struct/union member-array decay**, file-scope brace/string initializers with size inference, array-to-pointer parameter decay); **designated initializers** — `.member = value` member designators and `[index] = value` array index designators at both local and global scope, including **block-scope static aggregates** (stage 102), mixing designated and non-designated elements in the same list, with local arrays zero-filled before designated overwrites; **compound literals** — `(struct T){ … }`, `(int[]){ … }`, `(int[N]){ … }`, and `(int){ expr }` unnamed stack temporaries that are modifiable lvalues; compound literals support postfix `.field`/`[index]` chaining, can be passed directly to functions, can be assigned to matching variables, and have their address taken with `&`; string and character literals (tokenization, static-data emission, decoded simple/**hex `\xNN`/octal `\NNN`** escapes, and **adjacent string-literal concatenation**; `sizeof` of a string literal is its length+1); all bitwise operators, remainder, and all eleven compound assignment operators **on any modifiable lvalue**; prefix/postfix `++`/`--` **on any modifiable lvalue**; unary `&` (address-of) **on any addressable lvalue** including `&s.member`, `&p->member`, `&arr[i].member`, and **`&(T){ … }` compound literals**; `sizeof` (including multidimensional array types and arrow/subscripted member arrays; `sizeof(float)` = 4, `sizeof(double)` = 8); conditional (ternary) and comma operators (ternary supports FP conditions — stage 111); comma-separated init-declarator lists; file-scope global variable declarations, `extern` object declarations (NASM `extern` directives), and `global` directives for non-static file-scope definitions; storage-class specifiers `extern` and `static` at file scope **and block-scope `static` local variables** (scalars, **arrays, and struct/union aggregates** — stage 101) with RIP-relative addressing; **file-scope integer initializers accept full constant expressions** including `sizeof`, arithmetic, bitwise, shift, relational, equality, logical, and ternary operators — stage 100; **block-scope static scalar initializers** support the same operator set via `eval_const_init` in codegen — stages 103, 104; parenthesized and abstract declarators; function pointer declarations/assignment/indirect calls; `typedef` aliases for scalar/pointer/array/function-pointer and complete struct/union types; **`enum` declarations** with compile-time constant folding and **full integer constant expression values** (`FLAG = 1 << 0`, `COND = A > B ? A : B`, and arbitrary `?: || && == != < <= > >= << >> + - * / % & ^ |` combinations) plus **forward-declared enum tags** (stage 99); structs (definition, natural-alignment layout, `.`/`->` access with **general postfix chaining** like `p->tokens[i].kind`, brace initializers including char-member string init and **.member designated initializers**, whole-struct assignment/copy, pointer-to-struct mutation, nested structs, arrays of structs, typedef aliases, incomplete forward declarations); **named unions** (layout = max member size, member access, nested types, whole-union assignment, globals); **anonymous struct/union type declarations** (each definition is a unique type identified by pointer); **multi-file compilation** — `ccompiler file1.c file2.c …` compiles each source file independently through a fresh Lexer/Parser/CodeGen/AST pipeline with full per-file heap teardown. Diagnostics carry `<file>:<line>:<col>:` position prefixes on parser errors, support `--max-errors=N` multi-error collection with setjmp/longjmp recovery, expose a two-line `--version` build identifier with a `BuiltBy:` attribution field, and honor `-Werror`. A **multi-mode build workflow** (`build.sh`: `--mode=normal`/`--mode=bootstrap`/`--mode=fallback`/`--mode=self-host`) drives builds; the compiler's own source is strict ISO C99, and as of stage 92 the compiler is **fully self-hosting** — C0 (gcc-built) compiles itself to C1 and C1 compiles itself to C2, with all 1643 tests passing at each step (see `docs/self-compilation-report.md`). Stage 109 adds `float`/`double` types; stage 110 adds FP arithmetic and conversions; **stage 111 adds FP comparisons and boolean contexts** (`ucomiss`/`ucomisd`, NaN-correct comparison sequences, `emit_fp_bool_to_rax` for all control-flow conditions involving FP values). All 1643 tests pass at C0, C1, and C2.

What's still out of scope: **floating-point function parameters and return values** (stage 112 — SysV AMD64 passes FP args in `xmm0`–`xmm7`; currently FP values can be computed but not yet passed to or returned from user-defined functions via the FP register path); `va_arg` for floating-point types (stage 112; `float` arguments are promoted to `double` in variadic calls per C99 §6.5.2.2p6); `long double` (x87 80-bit; explicitly out of scope); multi-character and wide-character literals; compound literals at file scope; bit-fields, flexible array members; `volatile` code-generation semantics (currently parsed and tracked only); chained designators (`.a.b`, `.arr[0]`) and designated union init for non-first members; functions returning function pointers; pointer-to-array declarators (`(*p)[10]`); old-style (K&R) function definitions; `__attribute__` specifiers; `#pragma` (including `#pragma once`); GNU variadic macro extensions (`__VA_OPT__`, named variadic args, comma deletion); object-file (`.o`) emission and separate linking; header-only precompilation.
