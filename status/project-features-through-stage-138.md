What works today: a full preprocessing pipeline (line splicing, comment removal, `#include "file.h"` and `#include <file.h>`, object-like and function-like `#define` with parameter substitution and recursive expansion, variadic macros with `__VA_ARGS__`, macro stringification `#param`, token pasting `##`, `#undef`, conditional compilation `#ifdef`/`#ifndef`/`#if`/`#elif`/`#else`/`#endif` with full recursive-descent expression evaluation including `defined()`, macro expansion, arithmetic, bitwise, shift, logical, and comparison operators, `#error`, `#elifdef`/`#elifndef` branch directives (Stage 108), `#pragma` with unknown pragmas silently ignored and `#pragma once` supported (Stage 105), `_Pragma("string")` operator, `#line N "file"` directive, null directive (bare `#`), predefined macros `__FILE__`/`__LINE__`/`__DATE__`/`__TIME__`/`__STDC__`/`__STDC_VERSION__`/`__STDC_HOSTED__`/`__CLAUDEC99__`, ABI-reflecting macros `__x86_64__`/`__linux__`/`__unix__`/`__LP64__`/`_LP64`/`__CHAR_BIT__`/the full `__SIZEOF_*__` family, command-line `-D`/`-I`/`--sysroot` flags); an expanded set of stub system headers in `test/include/` covering `stdio.h` (complete stage-106 rewrite: opaque `FILE`, `EOF`, all standard streams, formatted I/O, character I/O, binary I/O, file positioning, and error-handling functions), `stddef.h`, `stdlib.h` (complete stage-106 rewrite plus `strtod` added at Stage 131 bootstrap: `malloc`, `calloc`, `free`, `realloc`, `exit`, `strtol`, `strtoul`, `strtod`, `div_t`/`ldiv_t`/`lldiv_t`, `EXIT_SUCCESS`/`EXIT_FAILURE`/`RAND_MAX`, `abs`/`labs`/`llabs`, `div`/`ldiv`/`lldiv`, `atoi`/`atol`/`atoll`, `strtoll`/`strtoull`, `bsearch`, `qsort`, `abort`, `atexit`, `rand`, `srand`, and more), `string.h` (complete stage-106 rewrite: `strlen`, `strcmp`, `strcpy`, `memcpy`, `memset`, `memcmp`, `strchr`, `strncat`, `strncmp`, `strncpy`, `strrchr`, `memmove`, `memchr`, `strcat`, `strcoll`, `strcspn`, `strspn`, `strpbrk`, `strstr`, `strtok`, `strerror`, `strxfrm`), `limits.h` (with `LLONG_MIN`/`LLONG_MAX`/`ULLONG_MAX`), `stdint.h` (exact-width/pointer-size/fast/least integer typedefs), `stdbool.h` (`bool`, `true`, `false`), `ctype.h` (full character classification including `isalpha`, `isdigit`, `isspace`, `iscntrl`, `isgraph`, `isprint`, `ispunct`), `errno.h` (`errno`, `ERANGE`, `EINVAL`, …), `time.h` (`time_t`, time functions), `setjmp.h` (non-local jumps), `stdarg.h` (`va_list` plus the `va_start`/`va_end`/`va_copy`/`va_arg` macros), `math.h` (double and float variants of `sin`/`cos`/`sqrt`/`pow`/`fabs`/`floor`/`ceil` and more; `M_PI`/`M_E`/`M_SQRT2` constants — Stage 112), and `assert.h` (NDEBUG-aware `assert` macro — Stage 107); statements (if/else, while/do-while/for **including C99 `for`-init declarations** `for (int i = 0; …)`, switch/case/default with **full constant-expression case labels** — integer/character literals, enum constants, and all of `* / % << >> + - & ^ |` with unary `~ !`, relational/equality/logical/ternary operators, and parentheses (stages 77, 99, 104), **switch on `long`/`long long`/`unsigned long long` discriminants** with 64-bit `cmp rax` path (Stage 121), break/continue/goto, blocks with shadowing, expression statements); the full signed integer hierarchy (`char`/`short`/`int`/`long`) plus all four unsigned variants plus `long long` and `unsigned long long`, the `signed` keyword forms and trailing-`int` forms; `_Bool` with value-normalization and integer promotion; **`float` and `double` floating-point types** with literals (decimal/exponent/`f`/`F`-suffix/leading-dot forms), stack and global variables, struct members with full SSE2 read/write/arithmetic/compound-assignment/increment-decrement support, and file-scope initialization from integer constants or **FP constant expressions at file scope** evaluated at compile time (Stages 109–112, 117–120, 125, 128); **floating-point arithmetic** — binary `+`/`-`/`*`/`/` with UAC, unary minus via `xorps`/`xorpd`, and inline type conversions (`cvtsi2ss`, `cvtsi2sd`, `cvtss2sd`, `cvtsd2ss`, `cvttss2si`, `cvttsd2si`) (Stage 110); **FP comparisons** — `ucomiss`/`ucomisd` with NaN-correct set sequences, FP boolean contexts in `if`/`while`/`for`/`do-while`/ternary conditions and logical NOT (Stage 111); **FP calling convention** — xmm0–xmm7 for `float`/`double` arguments and return values, non-variadic prologues move xmmN params to local slots, variadic prologues save xmm0–xmm7 to the 176-byte register-save area, `al` set to XMM count before variadic calls (Stage 112); **`va_arg` for `double`** via fp_offset and XMM register-save area path, with overflow-arg-area fallback (Stage 112); **variadic float→double promotion** per C99 §6.5.2.2p7 — `cvtss2sd` emitted in both stack-overflow and XMM-register paths for `float` variadic arguments (Stage 125); **`va_arg` for struct/union by value** — SysV AMD64 three-case classification: MEMORY class (>16 bytes, read from `overflow_arg_area` + `rep movsb` to scratch), 1-eightbyte register class (≤8 bytes, from `reg_save_area + gp_offset` or overflow), and 2-eightbyte register class (9–16 bytes, two consecutive 8-byte slots) (Stage 130); unsigned-aware arithmetic, division, shifts, and comparisons; integer promotions and Usual Arithmetic Conversions for mixed signed/unsigned and mixed FP/integer operands including LP64-specific rules; decimal, **`0x`/`0X` hexadecimal**, and **`0NNN` octal** (Stage 124; decimal-rewritten in lexer for NASM) integer literals with `U`/`L`/`UL`/`LL`/`ULL` suffixes and overflow-aware typing; void type and `void*` generic pointer; `const` qualifier on base-type scalars, **pointer-level `const` enforcement**, and **`const` in struct/union members and type-name contexts** (writes through `const*`, const-pointer reassignment, assignment to a const member directly/via subscript/through a pointer to a const-qualified object, and const-discard conversions all diagnosed); **minimal `volatile` handling** (tokenized, parsed everywhere `const` is, tracked on types, no codegen effect yet); **`restrict` qualifier** (tokenized, parsed at all pointer-qualifier positions, no semantic or codegen effect — Stage 106); **`inline` keyword** (tokenized, parsed, discarded — Stage 107); **`auto` storage class** (Stage 138: explicit `auto` at block scope accepted, tagged `SC_AUTO`, generates identical code to plain automatic storage); **`register` storage class** (Stage 138: `register` at block scope and in parameter declarations accepted, tagged `SC_REGISTER`; address-of rejected at compile time; otherwise identical code to automatic storage); casts; multi-function programs with forward declarations; **variadic function definitions** with `<stdarg.h>` support — `va_list`, `va_start`, `va_end`, `va_arg` for GP-class types (`int`/`unsigned`/`long`/`unsigned long`/`long long`/`unsigned long long`/pointer) via SysV AMD64 register save area with both register and overflow-stack paths, `va_arg` for `double` (Stage 112), `va_arg` for struct/union by value (Stage 130), `va_copy` via functional 24-byte struct copy (Stage 107); **struct and union parameters and return values passed by value** per the System V AMD64 ABI (register-class aggregates ≤16 bytes in registers, memory-class aggregates >16 bytes through a hidden pointer; shared call-layout helper for call sites and prologues; `rep movsb` block copies; whole-struct assignment and decl-init from struct rvalues, including subscript/dot/arrow/deref targets); **function calls with an arbitrary number of arguments** (full System V AMD64 stack-passing on both caller and callee sides, with 16-byte alignment); **full SysV AMD64 callee-saved register ABI** — `rbx` (Stage 122) and `r12`–`r15` (Stage 127) preserved in every function prologue and restored in all four epilogue paths, completing ABI compliance for all five callee-saved GP registers; pointers (declarations, `&`/`*` as rvalue and lvalue, assign-through-pointer, pointer parameters and return types, full pointer arithmetic including difference yielding `long`, `NULL`, null-constant `0` accepted as a pointer argument, logical `!` on pointer operands, **pointer relational operators** `<`/`<=`/`>`/`>=` with unsigned `setb`/`setbe`/`seta`/`setae` — Stage 118, and **pointer equality with non-null integer constants** — Stage 132); **pointer-to-array declarators** (Stage 135: `int (*row)[4]` and `int (*row)[]` valid parameter and local declarations; indexed access `(*row)[i]` via existing deref + subscript codegen); arrays (declarations, **multidimensional arrays** up to 8 dimensions in declarations/indexing/member access/`sizeof`, indexing, multi-level subscript, decay-to-pointer including **struct/union member-array decay**, file-scope brace/string initializers with size inference, array-to-pointer parameter decay, **array dimension constant expressions** enabling `int a[N*2]`/`int a[sizeof(int)*8]`/macro identifiers in bounds — Stage 115); **designated initializers** — `.member = value` member designators and `[index] = value` array index designators at both local and global scope, mixing designated and non-designated elements in the same list, with local arrays zero-filled before designated overwrites; **compound literals** — `(struct T){ … }`, `(int[]){ … }`, `(int[N]){ … }`, and `(int){ expr }` unnamed stack temporaries that are modifiable lvalues, with postfix `.field`/`[index]` chaining, address-of with `&`, and **file-scope pointer-type compound literals** (Stage 124); string and character literals (tokenization, static-data emission, decoded simple/**hex `\xNN`/octal `\NNN`** escapes, and **adjacent string-literal concatenation**; `sizeof` of a string literal is its length+1); all bitwise operators, remainder, and all eleven compound assignment operators **on any modifiable lvalue**; prefix/postfix `++`/`--` **on any modifiable lvalue**; unary `&` (address-of) **on any addressable non-register lvalue** including `&s.member`, `&p->member`, `&arr[i].member`, and **`&(T){ … }` compound literals** (Stage 138: address-of on `register` variables rejected); **`sizeof` (type and expression forms)** with **unsigned `size_t` result** per C99 §6.5.3.4 (Stage 131), correctly sized for pointer-arithmetic expressions `sizeof(p+1)` and `sizeof("str"+1)` (Stage 136), including multidimensional array types and arrow/subscripted member arrays, and `sizeof(type-name)` in constant-expression contexts (Stage 100); conditional (ternary) and comma operators; **`__func__` predefined string literal** (C99 §6.4.2.2 — Stage 124); comma-separated init-declarator lists; file-scope global variable declarations, `extern` object declarations (NASM `extern` directives), and `global` directives for non-static file-scope definitions; storage-class specifiers `extern` and `static` at file scope **and block-scope `static` local variables** (scalars/pointers stage 71; arrays/structs/unions Stage 101; full constant-expression initializers Stages 102–103); **tentative definitions** (C99 §6.9.2 — Stage 126): multiple file-scope declarations of the same variable without initializers merge, and the completing initializer resolves the group; **extern incomplete array declarations** (`extern int arr[];` before the defining declaration — Stage 129); **block-scope function declarations** (`void foo(void);` inside `{}`, registered with unknown arity so any call is accepted — Stage 129); **default argument promotions for no-prototype calls** (C99 §6.5.2.2p6 — Stage 133): `int f()` declarations set `is_no_prototype`; at call sites float→double promotion is applied (same `cvtss2sd` path as variadic promotion), and char/short→int promotion is automatic via `movsx`/`movzx` load instructions; parenthesized and abstract declarators; **function pointer declarations/assignment/indirect calls**; **functions returning function pointers** (Stage 137: `int (*get_adder())(int)` declarator form; `parse_declarator` parses `(*name())(params)`, `parse_external_declaration` builds the nested pointer-to-function return type); `typedef` aliases for scalar/pointer/array/function-pointer and complete struct/union types; **`enum` declarations** with compile-time constant folding and **full integer constant expression values** (including relational, equality, logical, and ternary operators — Stages 99, 104) plus **forward-declared enum tags** (Stage 99); structs (definition, natural-alignment layout, `.`/`->` access with **general postfix chaining** like `p->tokens[i].kind`, brace initializers including char-member string init and **.member designated initializers**, whole-struct assignment/copy, pointer-to-struct mutation, nested structs, arrays of structs, typedef aliases, incomplete forward declarations, and **float/double struct members** with full SSE2 arithmetic/compound-assignment/increment-decrement — Stages 117–120); **bit-field struct members** (Stage 134): named bit-fields (`unsigned int x : N`) packed into storage units with GCC-compatible layout, anonymous bit-fields (`: N` padding), and zero-width bit-fields (`: 0` forcing a new storage unit boundary); bit-field rvalue reads via load+shift+mask; bit-field lvalue writes via read-modify-write (load unit, clear bits, OR shifted new value, store); **flexible array members** (Stage 134): `type name[]` as the last named struct member, validated (must not be first/only member; no further named members may follow), excluded from `sizeof`, and accessed via existing array-decay-to-pointer codegen; **named unions** (layout = max member size, member access, nested types, whole-union assignment, globals); **anonymous struct/union type declarations** (each definition is a unique type identified by pointer); **multi-file compilation** — `ccompiler file1.c file2.c …` compiles each source file independently through a fresh Lexer/Parser/CodeGen/AST pipeline with full per-file heap teardown (`parser_free`, `codegen_free`, `lexer_free`, `ast_free`, `reset_error_state`); with `--max-errors=0` a compile error in one file does not prevent compilation of the remaining files. **Integer constant expressions** — a unified nine-level evaluator (`eval_const_expr`) covers case labels, enumerator values, array designator indices, array dimension bounds (Stage 115), file-scope integer global initializers (Stage 100), block-scope static scalar initializers (Stage 103), and bit-field width expressions (Stage 134), with the complete C99 operator set including relational/equality/logical/ternary (Stage 104) and `sizeof(type-name)` (Stage 100). **FP constant expressions** — `eval_fp_const_expr` evaluates `+`/`-`/`*`/`/` on FP literals at compile time for file-scope float/double global initializers (Stage 128). Diagnostics carry `<file>:<line>:<col>:` position prefixes on parser errors, support `--max-errors=N` multi-error collection with setjmp/longjmp recovery, expose a two-line `--version` build identifier with a `BuiltBy:` attribution field, and honor `-Werror`. A **multi-mode build workflow** (`build.sh`: `--mode=normal`/`--mode=bootstrap`/`--mode=fallback`/`--mode=self-host`, with bootstrap timeout guards) drives builds; the compiler's own source is strict ISO C99, and as of stage 92 the compiler is **fully self-hosting** — C0 (gcc-built) compiles itself to C1 and C1 compiles itself to C2, with all 1,970 tests passing at each step (see `docs/self-compilation-report.md`). All stages from 100 through 138 verified self-hosting. **Version: 13800000.**

What's still out of scope: `volatile` code-generation semantics (currently parsed and tracked only) and the `restrict` qualifier (parsed and discarded); `va_copy` with struct-containing `va_list` (the 24-byte copy is functional but untested in that context); multi-character and wide-character literals; non-pointer compound literals at file scope; chained designators (`.a.b`, `.arr[0]`) and designated union init for non-first members; old-style (K&R) function definitions; `__attribute__` specifiers; GNU variadic macro extensions (`__VA_OPT__`, named variadic args, comma deletion); object-file (`.o`) emission and separate linking; header-only precompilation.
