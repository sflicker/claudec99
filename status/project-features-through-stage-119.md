What works today: a full preprocessing pipeline (line splicing, comment removal, `#include "file.h"` and `#include <file.h>`, object-like and function-like `#define` with parameter substitution and recursive expansion, variadic macros with `__VA_ARGS__`, macro stringification `#param`, token pasting `##`, `#undef`, conditional compilation `#ifdef`/`#ifndef`/`#if`/`#elif`/`#else`/`#endif` with full recursive-descent expression evaluation including `defined()`, macro expansion, arithmetic, bitwise, shift, logical, and comparison operators, `#error`, `#elifdef`/`#elifndef` (stage 108), predefined macros `__FILE__`/`__LINE__`/`__DATE__`/`__TIME__`/`__STDC__`/`__STDC_VERSION__`/`__STDC_HOSTED__`/`__CLAUDEC99__` (with `__FILE__`/`__LINE__` fixed inside nested `#include` chains — stage 107), ABI-reflecting macros `__x86_64__`/`__linux__`/`__unix__`/`__LP64__`/`_LP64`/`__CHAR_BIT__`/the full `__SIZEOF_*__` family, command-line `-D`/`-I`/`--sysroot` flags); an expanded set of stub system headers in `test/include/` covering `stdio.h`, `stddef.h`, `stdlib.h`, `string.h`, `limits.h`, `stdint.h`, `stdbool.h`, `ctype.h`, `errno.h`, `time.h`, `setjmp.h`, `stdarg.h`, `assert.h`, and **`math.h`** (stage 112 — double and float math function declarations: `sin`, `cos`, `sqrt`, `pow`, `fabs`, `floor`, `ceil`, `round`, `trunc`, `exp`, `log`, `log2`, `log10`, `atan2`, `fmod`, `fmin`, `fmax`, `hypot`, and single-precision `*f` variants; constants `M_PI`, `M_E`, `M_SQRT2`, `M_LN2`, `M_LOG2E`); statements (if/else, while/do-while/for **including C99 `for`-init declarations**, switch/case/default with **full constant-expression case labels**, break/continue/goto, blocks with shadowing, expression statements); the full signed integer hierarchy (`char`/`short`/`int`/`long`) plus all four unsigned variants plus `long long` and `unsigned long long`; `_Bool` with value-normalization and integer promotion; unsigned-aware arithmetic, division, shifts, and comparisons; integer promotions and Usual Arithmetic Conversions; decimal and hexadecimal integer literals with `U`/`L`/`UL`/`LL`/`ULL` suffixes; **`float` and `double` scalar types** (stage 109) — 4-byte/8-byte SSE2 values in `xmm0`; float/double literals with deduplicated `.rodata` pool; `sizeof(float)` = 4, `sizeof(double)` = 8; `movss`/`movsd` load/store; **FP binary arithmetic** `addss`/`addsd`, `subss`/`subsd`, `mulss`/`mulsd`, `divss`/`divsd` (stage 110); **FP unary negation** via 16-byte-aligned sign-mask XOR; **Usual Arithmetic Conversions for FP**; **implicit and explicit FP conversions** (`cvtsi2ss`/`cvtsi2sd`/`cvtss2sd`/`cvtsd2ss`, truncating `cvttss2si`/`cvttsd2si`); **FP comparison operators** via `ucomiss`/`ucomisd` with NaN-correct sequences (stage 111); **FP boolean context** via `emit_fp_bool_to_rax`; **mixed FP/int comparisons**; **FP calling convention** (stage 112) — SysV AMD64 ABI: FP args in `xmm0`–`xmm7` (counted independently from GP args in `rdi`–`r9`); non-variadic function prologues move xmmN → local stack slot (`movss`/`movsd`); FP return values in `xmm0`; **variadic functions with FP extra args**: prologues save `xmm0`–`xmm7` to a 176-byte register-save area (48 GP + 128 XMM, 16-byte aligned), `al` set to XMM register count before variadic calls, `va_arg(ap, double)` reads `fp_offset` and loads from the XMM save area or overflow-arg-area; `va_arg(ap, float)` rejected per C99 §6.5.2.2p6; **FP struct member rvalue reads** (stage 117): `double`/`float` fields now correctly load into `xmm0` via `movsd`/`movss` when read via dot (`s.x`), arrow (`p->x`), or subscript-then-dot (`arr[i].x`); `expr_result_type()` now correctly reports `TYPE_DOUBLE`/`TYPE_FLOAT` for FP struct fields so binary operators take the SSE2 code path; the subscript-then-member (`arr[i].x`) base case is handled in both `expr_result_type()` and `sizeof_type_of_expr()`; **FP compound assignment on global struct fields** (stage 119): `expr_result_type()` and `sizeof_type_of_expr()` now add `codegen_find_global` fallbacks so FP field types resolve correctly for file-scope struct variables (not just locals); `emit_arrow_addr()` gains a global pointer-to-struct fallback so `gp->field` works as an lvalue when `gp` is a file-scope variable; compound assignment operators (`+=`, `-=`, `*=`, `/=`) on global struct FP fields now correctly emit SSE2 arithmetic rather than integer instructions on IEEE 754 bit patterns; **pointer relational comparisons** (stage 118): `<`, `<=`, `>`, `>=` on pointer operands emit unsigned `setb`/`setbe`/`seta`/`setae` setcc variants; `pointer < integer` is rejected (only `==`/`!=` against a null constant are valid per C99); void type and `void*` generic pointer; `const` qualifier with pointer-level const enforcement; `volatile` parsed and tracked (no codegen effect); `restrict` qualifier (parsed, silently discarded); `inline` function specifier (parsed, silently discarded); casts; multi-function programs with forward declarations; **variadic function definitions** with `<stdarg.h>` — `va_list`, `va_start`, `va_end`, `va_arg` for GP-class types and `double`, `va_copy`; **struct/union parameters and return values by value** per SysV AMD64 ABI; **function calls with arbitrary argument count** (full stack-passing); pointers, arrays (multidimensional up to 8 dimensions, decay, subscript, member-array decay); **designated initializers** and **compound literals**; string and character literals; all bitwise operators, compound assignment, prefix/postfix `++`/`--` on any modifiable lvalue; `sizeof`; conditional (ternary) and comma operators; file-scope globals, `extern` declarations, `static` at file scope and block scope; `typedef`; `enum` with full constant-expression values and forward declarations; structs, named/anonymous unions; multi-file compilation; diagnostics with position, `--max-errors=N`, `--version`, `-Werror`; multi-mode build workflow (`build.sh`); fully self-hosting since stage 92. **Stage 113** reorganizes the test suite into category subfolders with `find`-based discovery (no compiler changes). **Stage 114** fixes seven compiler bugs from legacy test import: FP array subscript read/write (movss/movsd), nested brace init for local and global multidimensional arrays, mixed FP/int ternary widening, and string literal subscript (`"abc"[2]`). **Stage 115** extends array dimension parsing to accept full C99 integer constant expressions (arithmetic, bitwise, shift, sizeof, enum constants, macro identifiers) instead of bare integer literals — four `parse_declarator`/`parse_type_name` sites now call `eval_const_expr()`. **Stage 116** fixes two codegen bugs for global struct arrays: uninitialized single-dimension struct/union arrays now correctly emit `resb (N × sizeof(struct))` in BSS (was `resd N`), and `char[N]` sub-arrays and struct fields initialized from string literals now emit inline bytes via `emit_string_as_bytes()` helper (was 8-byte pointer). **Stage 117** fixes three codegen bugs causing silent data corruption when reading `double`/`float` struct fields as rvalues: the rvalue load now emits `movsd xmm0, [rax]`/`movss xmm0, [rax]` instead of the erroneous `mov eax, [rax]`; `expr_result_type()` now returns the FP kind directly instead of going through `promote_kind()` which returned `TYPE_INT`; and the `AST_ARRAY_INDEX` base case for subscript-then-member access (`arr[i].x`) is handled in both `expr_result_type()` and `sizeof_type_of_expr()`. **Stage 118** adds pointer relational operators (`<`, `<=`, `>`, `>=`) on pointers: unsigned setcc variants (`setb`/`setbe`/`seta`/`setae`); rejects `pointer < integer`. **Stage 119** fixes six codegen bugs for FP compound assignment on file-scope struct fields: `codegen_find_global` fallbacks in `expr_result_type()`, `sizeof_type_of_expr()`, and `emit_arrow_addr()` ensure global struct FP fields select SSE2 arithmetic rather than integer instructions. All 1879 tests pass at C0, C1, and C2.

What's still out of scope: `long double` (x87 80-bit; explicitly out of scope); `va_arg` for struct-by-value types; multi-character and wide-character literals; compound literals at file scope; bit-fields, flexible array members; `volatile` code-generation semantics (currently parsed and tracked only); chained designators (`.a.b`, `.arr[0]`) and designated union init for non-first members; functions returning function pointers; pointer-to-array declarators (`(*p)[10]`); old-style (K&R) function definitions; `__attribute__` specifiers; GNU variadic macro extensions (`__VA_OPT__`, named variadic args, comma deletion); object-file (`.o`) emission and separate linking; header-only precompilation.
