What works today: a full preprocessing pipeline (line splicing, comment removal, `#include "file.h"` and `#include <file.h>`, object-like and function-like `#define` with parameter substitution and recursive expansion, variadic macros with `__VA_ARGS__`, macro stringification `#param`, token pasting `##`, `#undef`, conditional compilation `#ifdef`/`#ifndef`/`#if`/`#elif`/`#else`/`#endif` with full recursive-descent expression evaluation including `defined()`, macro expansion, arithmetic, bitwise, shift, logical, and comparison operators, `#error`, predefined macros `__FILE__`/`__LINE__`/`__DATE__`/`__TIME__`/`__STDC__`/`__STDC_VERSION__`/`__STDC_HOSTED__`/`__CLAUDEC99__`, ABI-reflecting macros `__x86_64__`/`__linux__`/`__unix__`/`__LP64__`/`_LP64`/`__CHAR_BIT__`/`__SIZEOF_CHAR__`/`__SIZEOF_SHORT__`/`__SIZEOF_INT__`/`__SIZEOF_LONG__`/`__SIZEOF_POINTER__`/`__SIZEOF_SIZE_T__`/`__SIZEOF_LONG_LONG__`, command-line `-D` and `-I` flags); stub system headers in `test/include/` covering `stdio.h`, `stddef.h`, `stdlib.h`, `string.h`, `limits.h` (with `LLONG_MIN`/`LLONG_MAX`/`ULLONG_MAX`), `stdint.h` (exact-width signed/unsigned typedefs `int8_t`…`uint64_t` and pointer-size/fast/least variants), and `stdbool.h` (`bool`, `true`, `false`); statements (if/else, while/do-while/for, switch/case/default, break/continue/goto, blocks with shadowing, expression statements); the full signed integer hierarchy (`char`/`short`/`int`/`long`) plus all four unsigned variants plus `long long` and `unsigned long long` (8 bytes, alignment 8; codegen identical to `long`), `signed` keyword forms (`signed char`, `signed short`, `signed int`, `signed long`, plain `signed` = int), trailing-`int` forms (`short int`, `long int`, etc.); `_Bool` type with value-normalization (any nonzero becomes 1, zero stays 0) and integer promotion; unsigned-aware arithmetic, division, right shift, and comparisons; integer promotions and Usual Arithmetic Conversions for mixed signed/unsigned operands including LP64-specific long-vs-unsigned-int rules; `U`/`L`/`UL`/`LL`/`ULL` integer literal suffixes with overflow-aware literal typing; void type and `void*` generic pointer (implicit conversion to/from any non-function pointer type; dereferencing/arithmetic/subscripting `void*` rejected); minimal `const` qualifier (base-type scalars, direct assignment to const lvalues rejected); casts `(<type>) <expr>`; multi-function programs with forward declarations, variadic function declarations and calls (≥ fixed parameter count required; `xor eax, eax` per SysV AMD64 convention), and SysV-AMD64 calls (≤6 args) with boundary conversions; pointers (declarations, `&`/`*` as rvalue and lvalue, assign-through-pointer, pointer parameters and return types, full pointer arithmetic including difference yielding `long`, `NULL`); arrays (declarations, indexing, multi-level subscript `a[i][j]`, decay-to-pointer, file-scope brace/string initializers with size inference, `char`-array initialization from string literals); string literals (tokenization, static-data emission, decay to `char*`, decoded escapes); character literals (full simple-escape set, `int` typing in any integer context); all bitwise operators (`&` `^` `|` `~` `<<` `>>`), remainder `%`, all eleven compound assignment operators; `sizeof` (type names, expressions, whole arrays/structs — operand not evaluated; `sizeof(void)` rejected); conditional (ternary) operator; comma operator; comma-separated init-declarator lists; file-scope global variable declarations (`.bss`/`.data`, RIP-relative addressing); storage-class specifiers `extern` and `static`; parenthesized declarators; abstract pointer parameters in prototypes; function pointer declarations/assignment/indirect calls (`fp(args)` and `(*fp)(args)`); `typedef` aliases for scalar/pointer/array/function-pointer and complete struct types with block-scope shadowing; `enum` declarations with compile-time constant folding; structs (definition, natural-alignment layout, `sizeof`, `.`/`->` member access in rvalue and lvalue contexts, brace initializer lists with zero-fill, whole-struct assignment and copy-initialization, pointer-to-struct parameters with field mutation, nested structs, arrays of structs, typedef aliases, incomplete forward declarations for self-referential types).

What's still out of scope: floating-point types (`float`, `double`); `for`-loop initializer declarations; defining variadic functions (`va_list`/`va_start`/`va_arg`/`va_end`/`<stdarg.h>`); octal and hex character escapes (`'\123'`, `'\x41'`); multi-character and wide-character literals; function calls with more than 6 arguments; struct by-value parameters and return values; block-scope `static` local variables; unions, bit-fields, flexible array members, compound literals; pointer-level `const` enforcement (writes through `const*`, conversion const-correctness); `typedef enum`; enum constant expressions beyond integer/character literals; functions returning function pointers; `#pragma` (including `#pragma once`); `#elifdef`/`#elifndef`; GNU variadic macro extensions (`__VA_OPT__`, named variadic args, comma deletion).
