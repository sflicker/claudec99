section .text
global main
main:
    push rbp
    mov rbp, rsp
    sub rsp, 16
    lea rax, [rel Lstr0]
    mov [rbp - 8], rax
    mov eax, 0
    mov edi, eax
    mov eax, 60
    syscall
section .rodata
Lstr0:
    db 104, 105, 0
