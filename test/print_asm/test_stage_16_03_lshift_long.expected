section .text
global f
f:
    push rbp
    mov rbp, rsp
    sub rsp, 16
    mov rax, 8
    mov [rbp - 8], rax
    mov rax, [rbp - 8]
    push rax
    mov eax, 2
    pop rcx
    xchg rax, rcx
    shl rax, cl
    mov rsp, rbp
    pop rbp
    ret
