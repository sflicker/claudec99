section .text
global main
main:
    push rbp
    mov rbp, rsp
    sub rsp, 32
    mov [rbp - 8], rbx
    mov rax, 20
    mov [rbp - 16], rax
    mov rax, 6
    mov [rbp - 24], rax
    mov rax, [rbp - 16]
    push rax
    mov rax, [rbp - 24]
    pop rcx
    xchg rax, rcx
    cqo
    idiv rcx
    mov rax, rdx
    mov rbx, [rbp - 8]
    mov rsp, rbp
    pop rbp
    ret
section .data
Lstatic_main___func__0: db 109, 97, 105, 110, 0

section .note.GNU-stack noalloc noexec nowrite progbits
