section .text
global main
main:
    push rbp
    mov rbp, rsp
    sub rsp, 16
    mov [rbp - 8], rbx
    mov eax, 6
    push rax
    mov eax, 3
    pop rcx
    or eax, ecx
    mov rbx, [rbp - 8]
    mov rsp, rbp
    pop rbp
    ret

section .note.GNU-stack noalloc noexec nowrite progbits
