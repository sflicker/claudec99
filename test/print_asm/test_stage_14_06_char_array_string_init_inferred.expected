section .text
global main
main:
    push rbp
    mov rbp, rsp
    sub rsp, 16
    mov byte [rbp - 3], 104
    mov byte [rbp - 2], 105
    mov byte [rbp - 1], 0
    mov eax, 0
    mov edi, eax
    mov eax, 60
    syscall
