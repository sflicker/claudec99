section .text
global f
f:
    push rbp
    mov rbp, rsp
    sub rsp, 32
    mov [rbp - 8], rbx
    mov rax, 6
    mov [rbp - 16], rax
    mov rax, 3
    mov [rbp - 24], rax
    mov rax, [rbp - 16]
    push rax
    mov rax, [rbp - 24]
    pop rcx
    and rax, rcx
    mov rbx, [rbp - 8]
    mov rsp, rbp
    pop rbp
    ret
section .data
Lstatic_f___func__0: db 102, 0

section .note.GNU-stack noalloc noexec nowrite progbits
