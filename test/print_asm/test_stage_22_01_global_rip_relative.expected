section .bss
global g
g: resd 1
section .text
global main
main:
    push rbp
    mov rbp, rsp
    mov eax, 42
    mov [rel g], eax
    mov eax, [rel g]
    mov rsp, rbp
    pop rbp
    ret

section .note.GNU-stack noalloc noexec nowrite progbits
