section .text
global f
f:
    push rbp
    mov rbp, rsp
    sub rsp, 16
    mov rax, 2
    mov [rbp - 8], rax
    mov rax, [rbp - 8]
    not rax
    mov rsp, rbp
    pop rbp
    ret
global main
main:
    push rbp
    mov rbp, rsp
    call f
    mov rsp, rbp
    pop rbp
    ret
