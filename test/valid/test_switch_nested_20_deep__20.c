int main() {
    int x = 1;
    int r = 0;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    switch (x) { case 1: r++;
    }}}}}}}}}}
    }}}}}}}}}}
    return r;
}
